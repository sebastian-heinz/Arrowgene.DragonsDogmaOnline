namespace Arrowgene.Ddon.Shared.Entity.Structure.CDataLobbyChatMsgType {
    // Check packet captures to figure this out
    public enum Type : byte
    {
        Say = 0,
        Shout = 1,
        // Party = ?,
        // Tell = ?,
        Clan = 7
        // Group = ?,
        // EntryBoard = ?,
        // Memo = ?
    }

}